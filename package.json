{
  "private": true,
  "name": "swami",
  "version": "1.0.0",
  "description": "A template for kick starting a project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "swami",
  "license": "DTU",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
